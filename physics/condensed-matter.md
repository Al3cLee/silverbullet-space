Pages tagged with `physics/condensed-matter` are collected here.

${template.each(
query [[from index.tag "page" where table.includes(tags, "physics/condensed-matter")]], template.new[==[* [[${name}]] ]==]
)}
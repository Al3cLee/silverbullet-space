---
finished: false
---

#computer-science/julia

A fast and modern language server for `julia`. Documentation is [here](https://aviatesk.github.io/JETLS.jl/release/). Instead of a `julia` package (like `LanguageServer.jl`), this is a binary that can be installed and called from the terminal.
